spring:
  datasource:
    url: jdbc:mysql://localhost:3306/singlebungle_test?createDatabaseIfNotExist=true
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop   # 테스트 실행 시마다 스키마를 새로 생성하고 종료 시 삭제
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

